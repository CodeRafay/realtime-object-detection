# 🏗️ System Architecture

## High-Level Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         User's Browser                           │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                      React Application                      │  │
│  │                                                             │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │  │
│  │  │   Camera     │  │   Controls   │  │     FPS      │    │  │
│  │  │     View     │  │              │  │   Monitor    │    │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘    │  │
│  │                                                             │  │
│  │  ┌──────────────────────────────────────────────────────┐  │  │
│  │  │              Bounding Box Canvas                      │  │  │
│  │  │          (Detection Visualization)                    │  │  │
│  │  └──────────────────────────────────────────────────────┘  │  │
│  │                                                             │  │
│  │  ┌─────────────────┐         ┌───────────────────────┐    │  │
│  │  │   useCamera     │         │  useObjectDetection   │    │  │
│  │  │   (Hook)        │         │      (Hook)           │    │  │
│  │  └─────────────────┘         └───────────────────────┘    │  │
│  │          │                               │                  │  │
│  └──────────┼───────────────────────────────┼─────────────────┘  │
│             │                               │                    │
│  ┌──────────▼───────────┐       ┌──────────▼───────────────┐   │
│  │   Browser Camera     │       │   ONNX Runtime Web       │   │
│  │         API          │       │   (WebGPU/WASM)          │   │
│  └──────────────────────┘       └──────────────────────────┘   │
│                                              │                   │
│                                   ┌──────────▼──────────┐        │
│                                   │   GPU/CPU Hardware  │        │
│                                   └─────────────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Architecture

```
┌─────────────┐
│   Camera    │
│  Hardware   │
└──────┬──────┘
       │ Video Stream
       ▼
┌──────────────┐
│   Video      │
│   Element    │
└──────┬───────┘
       │ Frame
       ▼
┌──────────────────┐
│  Preprocessing   │
│  - Resize        │
│  - Normalize     │
│  - CHW Format    │
└──────┬───────────┘
       │ Tensor [1,3,640,640]
       ▼
┌──────────────────┐
│  ONNX Runtime    │
│  - WebGPU/WASM   │
│  - YOLOv8n       │
└──────┬───────────┘
       │ Output [1,84,8400]
       ▼
┌──────────────────┐
│ Postprocessing   │
│  - Parse Output  │
│  - NMS           │
│  - Scale Boxes   │
└──────┬───────────┘
       │ Detections Array
       ▼
┌──────────────────┐
│  Canvas Drawing  │
│  - Bounding Boxes│
│  - Labels        │
│  - Confidence    │
└──────────────────┘
```

## Component Hierarchy

```
App.jsx
├── CameraView.jsx
│   ├── <video> element
│   └── BoundingBoxCanvas.jsx
│       └── <canvas> element
│
├── Controls.jsx
│   ├── Model controls
│   └── Camera selector
│
├── FPSMonitor.jsx
│
└── InfoBox (inline)
```

## Hook Dependencies

```
useCamera
├── useState (stream, error, devices, etc.)
├── useEffect (device enumeration)
├── useEffect (cleanup)
├── useRef (videoRef)
└── Functions:
    ├── startCamera()
    ├── stopCamera()
    ├── switchCamera()
    └── changeCamera()

useObjectDetection
├── useState (model, isLoading, isRunning, fps, etc.)
├── useEffect (cleanup animation frame)
├── useRef (animationFrameRef, fpsCounterRef)
├── useCallback (initializeModel)
├── useCallback (detectObjects)
├── useCallback (startDetection)
└── useCallback (stopDetection)
```

## Utility Module Structure

```
utils/
│
├── constants.js
│   ├── COCO_CLASSES[]
│   ├── COLORS[]
│   ├── MODEL_CONFIG{}
│   └── CAMERA_CONFIG{}
│
├── modelLoader.js
│   ├── initializeORT()
│   ├── loadModel()
│   ├── runInference()
│   └── warmupModel()
│
├── preprocessing.js
│   ├── preprocessImage()
│   └── preprocessImageOffscreen()
│
└── postprocessing.js
    ├── calculateIoU()
    ├── nonMaxSuppression()
    ├── processOutput()
    └── scaleDetections()
```

## Execution Flow

```
1. Application Loads
   └── React renders UI
       └── useCamera initializes
           └── Camera stream starts

2. User Clicks "Load Model"
   └── initializeModel()
       ├── initializeORT()
       ├── loadModel()
       └── warmupModel()

3. User Clicks "Start Detection"
   └── startDetection()
       └── detectLoop() [recursive with requestAnimationFrame]
           ├── Get video frame
           ├── preprocessImage()
           │   ├── Resize & pad
           │   ├── Normalize
           │   └── Convert to CHW
           ├── runInference()
           │   └── ONNX Runtime executes
           ├── processOutput()
           │   ├── Parse predictions
           │   ├── Filter by confidence
           │   └── Apply NMS
           ├── scaleDetections()
           ├── Update state (detections, fps)
           ├── Canvas redraws
           └── Schedule next frame

4. User Clicks "Stop Detection"
   └── stopDetection()
       └── Cancel animation frame
```

## State Management

```
App Level State:
├── Camera State (useCamera)
│   ├── stream
│   ├── devices
│   ├── selectedDeviceId
│   ├── error
│   └── isInitialized
│
└── Detection State (useObjectDetection)
    ├── model
    ├── isLoading
    ├── isRunning
    ├── fps
    ├── detections
    ├── executionProvider
    └── error
```

## Performance Optimization Points

```
1. Preprocessing
   └── OffscreenCanvas when available
       └── Faster than regular canvas

2. Inference
   └── WebGPU execution provider
       └── GPU acceleration
           └── Fallback to WebAssembly

3. Rendering
   └── requestAnimationFrame
       └── Synced with display refresh
           └── No unnecessary renders

4. Memory Management
   └── Reuse tensor buffers
       └── Minimal garbage collection
           └── Stable performance
```

## Browser API Usage

```
┌────────────────────────────────┐
│      Browser APIs Used          │
├────────────────────────────────┤
│ • navigator.mediaDevices       │ Camera access
│ • getUserMedia()                │ Video stream
│ • enumerateDevices()            │ Device list
│ • HTMLVideoElement              │ Video display
│ • HTMLCanvasElement             │ Drawing
│ • CanvasRenderingContext2D      │ 2D graphics
│ • OffscreenCanvas               │ Performance
│ • requestAnimationFrame         │ Smooth loop
│ • performance.now()             │ FPS tracking
│ • navigator.gpu                 │ WebGPU check
│ • WebGPU API                    │ GPU compute
└────────────────────────────────┘
```

## Deployment Architecture

```
┌─────────────────────────────────────────┐
│         CDN / Static Hosting             │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │         Static Assets               │ │
│  │  • index.html                       │ │
│  │  • JavaScript bundle                │ │
│  │  • CSS files                        │ │
│  │  • ONNX model (6MB)                 │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │      Edge Locations (Global)        │ │
│  │  • Netlify / Vercel / Cloudflare   │ │
│  │  • Automatic HTTPS                  │ │
│  │  • Instant scaling                  │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│          User's Browser                  │
│  • Downloads app (one-time)              │
│  • Caches model                          │
│  • Runs 100% locally                     │
│  • No further server contact             │
└─────────────────────────────────────────┘
```

## Security & Privacy Architecture

```
┌──────────────────────────────────────┐
│       Privacy Guarantees              │
├──────────────────────────────────────┤
│                                       │
│  ┌─────────────────────────────┐    │
│  │    User's Device              │    │
│  │  ┌─────────────────────────┐ │    │
│  │  │   Camera                 │ │    │
│  │  └──────────┬───────────────┘ │    │
│  │             │ Local only      │    │
│  │             ▼                 │    │
│  │  ┌─────────────────────────┐ │    │
│  │  │   Browser Memory         │ │    │
│  │  │  • Video frames          │ │    │
│  │  │  • Model inference       │ │    │
│  │  │  • Detections           │ │    │
│  │  └─────────────────────────┘ │    │
│  │             │                 │    │
│  │             ▼                 │    │
│  │  ┌─────────────────────────┐ │    │
│  │  │   Screen Display         │ │    │
│  │  └─────────────────────────┘ │    │
│  └─────────────────────────────┘    │
│                                       │
│  ❌ No Network Transmission           │
│  ❌ No Server Processing              │
│  ❌ No Data Storage                   │
│  ❌ No Tracking                       │
│  ❌ No Cookies                        │
│                                       │
└──────────────────────────────────────┘
```

## Technology Stack Layers

```
┌─────────────────────────────────────┐
│        Presentation Layer            │  React Components
├─────────────────────────────────────┤
│        Business Logic Layer          │  Custom Hooks
├─────────────────────────────────────┤
│        Data Processing Layer         │  Utility Functions
├─────────────────────────────────────┤
│        AI Inference Layer            │  ONNX Runtime Web
├─────────────────────────────────────┤
│        Execution Layer               │  WebGPU / WebAssembly
├─────────────────────────────────────┤
│        Hardware Layer                │  GPU / CPU
└─────────────────────────────────────┘
```

---

This architecture provides:

- ✅ Clear separation of concerns
- ✅ Modular and maintainable code
- ✅ High performance
- ✅ Privacy by design
- ✅ Scalable to unlimited users
- ✅ Zero operational cost
